# All-local Ansible configuration for the MLOps platform.
# Run ansible commands from this directory (ansible/), e.g.:
#   ansible-playbook --syntax-check playbooks/site.yml
[defaults]
inventory = inventory/hosts.yml
roles_path = roles
host_key_checking = False
stdout_callback = yaml
