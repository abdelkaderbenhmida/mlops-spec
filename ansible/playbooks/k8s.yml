---
# Kubernetes cluster playbook: runs common, docker, kubernetes on local hosts.
- name: Bootstrap local servers
  hosts: k8s_local
  become: true
  roles:
    - common
    - docker

- name: Configure Kubernetes cluster
  hosts: k8s_local
  become: true
  roles:
    - kubernetes